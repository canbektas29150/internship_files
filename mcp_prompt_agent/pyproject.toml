[project]
name = "mcp-prompt-agent-demo"
version = "0.1.0"
description = "Small MCP prompt server demo for OpenAI Agents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.9.0",
    "openai-agents>=0.0.17",
]

[tool.uv]
package = false
